<!DOCTYPE html>
<html>
  <head>
    <title>神社御祈祷予約システム</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-100">
    <!-- ヘッダー -->
    <header class="bg-indigo-800 text-white">
      <div class="container mx-auto px-4 py-4">
        <div class="flex justify-between items-center">
          <%= link_to "神社御祈祷予約", root_path, class: "text-2xl font-bold" %>
          <nav>
            <div class="space-x-4">
              <%= link_to "ホーム", root_path, class: "hover:text-indigo-200" %>
              <%= link_to "新規予約", new_reservation_path, class: "hover:text-indigo-200" %>
              <%= link_to "予約確認", reservations_path, class: "hover:text-indigo-200" %>
            </div>
          </nav>
        </div>
      </div>
    </header>

    <!-- フラッシュメッセージ -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= notice %></span>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= alert %></span>
      </div>
    <% end %>

    <!-- メインコンテンツ -->
    <main>
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="bg-gray-800 text-white mt-12">
      <div class="container mx-auto px-4 py-6">
        <p class="text-center">&copy; 2024 神社御祈祷予約システム All Rights Reserved.</p>
      </div>
    </footer>
  </body>
</html>
