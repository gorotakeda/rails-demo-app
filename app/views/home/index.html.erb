<div class="container mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold text-center mb-8">神社御祈祷予約システム</h1>

  <div class="grid md:grid-cols-2 gap-8">
    <!-- 新規予約セクション -->
    <div class="bg-white p-6 rounded-lg shadow-lg">
      <h2 class="text-xl font-semibold mb-4">新規御祈祷予約</h2>
      <p class="mb-4">ご希望の御祈祷の予約を承ります。</p>
      <%= link_to "新規予約", new_reservation_path, class: "inline-block bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700" %>
    </div>

    <!-- 予約確認セクション -->
    <div class="bg-white p-6 rounded-lg shadow-lg">
      <h2 class="text-xl font-semibold mb-4">予約確認</h2>
      <p class="mb-4">現在の予約状況を確認できます。</p>
      <%= link_to "予約一覧", reservations_path, class: "inline-block bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700" %>
    </div>
  </div>

  <!-- 御祈祷の種類一覧 -->
  <div class="mt-12">
    <h2 class="text-2xl font-semibold mb-6">御祈祷の種類</h2>
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
      <% PrayerType.active.each do |prayer_type| %>
        <div class="bg-white p-4 rounded-lg shadow">
          <h3 class="text-lg font-semibold mb-2"><%= prayer_type.name %></h3>
          <p class="text-gray-600 mb-2"><%= prayer_type.description %></p>
          <p class="text-indigo-600 font-semibold">
            料金: <%= number_to_currency(prayer_type.price, unit: '¥', precision: 0) %>
          </p>
        </div>
      <% end %>
    </div>
  </div>

  <!-- お知らせセクション -->
  <div class="mt-12 bg-white p-6 rounded-lg shadow-lg">
    <h2 class="text-2xl font-semibold mb-4">お知らせ</h2>
    <ul class="list-disc list-inside space-y-2">
      <li>予約は3ヶ月先まで受け付けております。</li>
      <li>当日のキャンセルはご遠慮ください。</li>
      <li>ご不明な点がございましたら、お電話にてお問い合わせください。</li>
    </ul>
  </div>
</div>
